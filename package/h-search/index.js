// Importing the h-search.vue component
import HSearch from './h-search.vue';

// Defining the install function
function install(Vue) {
  Vue.component('h-search', HSearch);
}

// Exporting the install function
export default install;

// Outputting the registration of the h-search.vue component